# EMILY'S ROUTE
homepage:
    path:   '/'
    defaults:  { _controller: AppBundle:Home:index }

# CHRIS'S''S'S'S'S'S'S'SS'S' ROUTES
account:
  path: '/home'
  defaults: {_controller: AppBundle:Home:index}


section_new:
  path: '/home/{userId}/section/new'
  defaults: {_controller: AppBundle:Section:newSection}


section_insert:
  path: '/home/{userId}/section/insert/{courseId}/{name}/{students}/{semester}/{year}/{start_time}/{end_time}/{is_deleted}/{is_public}'
  defaults: {_controller: AppBundle:Section:insertSection, name: "hey", semester: "Dr. G", year: "Node", start_time: "is way better", end_time: "than anything", is_deleted: "you have used", is_public: ":)", students: "love, Chris Brauns"}


section:
  path: '/home/{userId}/section/{sectionId}'
  defaults: {_controller: AppBundle:Section:section}

  
assignment:
  path: '/home/{userId}/section/{sectionId}/assignment/{assignmentId}/problem/{problemId}'
  defaults:
    _controller: AppBundle:Problems:problems
    problemId: 0
        

# EMMETT'S ROUTE
upload:
  path: "/courses/assignments/problems/{problem_id}/upload"
  defaults: {_controller: AppBundle:Upload:upload}
  requirements:
        problem_id:     '\d+'

# TIMOTHY'S ROUTES
submit:
    path: '/compilation/submit/{problem_id}/{language_id}/{submitted_filename}/{main_class}/{package_name}'
    defaults:
        _controller:    AppBundle:Compilation:submit
        main_class:     ''
        package_name:   ''
    requirements:
        problem_id:     '\d+'
        language_id:    '\d+'

submit_backup:
    path: '/compilation/submit_backup/{problem_id}/{language_id}/{submitted_filename}/{main_class}/{package_name}'
    defaults:
        _controller:    AppBundle:Compilation:submit_backup
        main_class:     ''
        package_name:   ''
    requirements:
        problem_id:  '\d+'
        language_id: '\d+'

submission_results:
    path: '/compilation/submission_results/{submission_id}'
    defaults: {_controller: AppBundle:Compilation:submission}
    requirements:
        submission_id: '\d+'

# OAUTH'S ROUTES
hwi_oauth_redirect:
    resource: "@HWIOAuthBundle/Resources/config/routing/redirect.xml"
    prefix:   /connect

hwi_oauth_connect:
    resource: "@HWIOAuthBundle/Resources/config/routing/connect.xml"
    prefix:   /connect

hwi_oauth_login:
    resource: "@HWIOAuthBundle/Resources/config/routing/login.xml"
    prefix:   /login

user_logout:
    path: '/user/logout'

user_login:
  path: '/user/login'
  defaults: {_controller: AppBundle:Default:index}
